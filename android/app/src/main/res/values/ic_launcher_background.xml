<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Same navy as the accent colour in the app's stylesheet, so the
         adaptive icon and the legacy square icon are one design rather
         than two. -->
    <color name="ic_launcher_background">#1D4E6F</color>
</resources>
