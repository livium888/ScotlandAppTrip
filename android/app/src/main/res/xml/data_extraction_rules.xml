<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ equivalent of allowBackup=false. The app's storage holds API
     keys, so neither cloud backup nor device-to-device transfer should carry
     it. Trips move via the app's own Export, which strips the keys. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
